import 'package:flutter/material.dart';

class AppStyle {
  static const tempStyle = TextStyle(fontSize: 72, color: Colors.white);

  static const countryStyle = TextStyle(fontSize: 42, color: Colors.white);

  static const additionalStyle = TextStyle(fontSize: 24, color: Colors.white);

  static const errorStyle =
      TextStyle(fontSize: 42, color: Color.fromRGBO(136, 8, 8, 1));
}
